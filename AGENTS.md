# Agent Instructions

- Use conda to activate the `suite2p` environment before running commands.
- Download demo data to `/mnt/nas02/Dataset/suite2p`.
- iteratively run the agent; auto approve enabled